apiVersion: v1
kind: Service
metadata:
  name: {{ include "emojivoto.fullname" . }}
  labels:
    {{- include "emojivoto.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "emojivoto.selectorLabels" . | nindent 4 }}
